# HABITUS KOREA 전자투표 홈페이지

HABITUS KOREA의 **전자투표 안내 · 현장 사진 홍보 · 홍보자료 제공 · 온라인 문의 접수**를 위한 정적 웹사이트입니다.
별도의 빌드 과정 없이 브라우저에서 바로 동작하며, GitHub Pages 등으로 즉시 배포할 수 있습니다.

> **에이스 종합관리 — 지점사업소 관리단 구성 전략 앱**은 별도 페이지 **`manage.html`** 로 제공됩니다. 아래 [관리단 구성 전략 앱](#에이스-종합관리--지점사업소-관리단-구성-전략-앱) 참고.


## 주요 기능

- **전자투표 바로가기** — 외부 전자투표 시스템으로 연결되는 버튼
- **현장 갤러리** — 현장 사진을 올려 홍보 (클릭 시 확대 보기)
- **홍보자료** — 포스터·브로슈어, **회사 CI(로고)**, **개인정보처리 위탁계약서(위탁서)** 등을 미리보기하고 다운로드
- **견적서** — **당사 견적서 받기**(다운로드) + **타사 견적서 업로드**(파일 첨부 접수)
- **문의하기** — 이름/이메일/내용을 작성하면 담당자 메일로 전송
- **회사 CI 적용** — 헤더/푸터에 (주)HABITUS KOREA 공식 로고 표시

## 폴더 구조

```
.
├── index.html          # 메인 페이지
├── css/style.css       # 스타일
├── js/main.js          # 동작 (갤러리·미리보기·문의 폼)
└── assets/
    ├── images/         # 현장 갤러리 사진
    └── resources/      # 홍보자료(포스터, 브로슈어 등)
```

## 사용 방법

### 1. 전자투표 주소 변경
`index.html`에서 `id="voteLink"` 버튼의 `href`를 실제 전자투표 시스템 주소로 변경하세요.

### 2. 현장 사진 추가
1. 사진 파일을 `assets/images/` 폴더에 넣습니다.
2. `index.html`의 `현장 갤러리` 영역에서 아래 항목을 복사해 `src`와 캡션을 수정합니다.

```html
<figure class="gallery-item">
  <img src="assets/images/내사진.jpg" alt="설명" loading="lazy" />
  <figcaption>사진 설명</figcaption>
</figure>
```

### 3. 홍보자료 추가
1. 자료 파일(이미지 또는 PDF)을 `assets/resources/` 폴더에 넣습니다.
2. `index.html`의 `홍보자료` 영역에서 항목을 복사한 뒤
   - `data-file` : 파일 경로
   - `data-type` : `image` 또는 `pdf`
   - `다운로드` 링크의 `href` 를 같은 파일 경로로 맞춰줍니다.

```html
<article class="resource-item" data-file="assets/resources/자료.pdf" data-type="pdf">
  ...
  <a class="btn btn-sm btn-primary" href="assets/resources/자료.pdf" download>다운로드 ↓</a>
</article>
```

> 현재 들어있는 이미지들은 자리표시용(placeholder) SVG입니다. 실제 사진/자료로 교체해 사용하세요.

### 4. 견적서 설정
- **당사 견적서**: `assets/resources/habitus-quote.svg`(예시 양식)를 실제 견적서 파일로 교체하고, `index.html`의 견적서 다운로드 링크 `href`를 맞춰주세요.
- **타사 견적서 업로드**: 기본값은 파일을 선택하면 메일 작성 창이 열려 직접 첨부·전송하는 방식입니다.
  실제 자동 업로드(서버 저장)를 원하시면 `js/main.js`의 `UPLOAD_ENDPOINT`에 폼 처리 서비스(예: [Formspree](https://formspree.io)) 엔드포인트 주소를 입력하세요. 입력 시 파일이 해당 서비스로 직접 전송됩니다.

### 5. 회사 CI(로고) / 위탁서 교체
- 로고: `assets/images/logo.png` (헤더·푸터·OG 이미지에 사용). 교체 시 같은 파일명을 사용하면 자동 반영됩니다.
- 위탁서: `assets/resources/habitus-personal-info-consignment.hwp` (한글 파일).

### 6. 문의 접수 이메일 변경
`js/main.js`의 `TO_EMAIL` 값과 `index.html` 연락처의 이메일 주소를 원하는 주소로 변경하세요.
문의 폼은 방문자의 메일 작성 창(mailto)을 열어 전송하는 방식입니다.

## 에이스 종합관리 — 지점사업소 관리단 구성 전략 앱

내부 운영용 모바일 웹앱입니다. **`manage.html`** 한 페이지로 동작하며(`…/manage.html` 접속),
PDF 전략 문서(「지점사업소 관리단 구성 전략」, 에이스 종합관리㈜)를 그대로 옮긴 구조입니다.

### 화면 구성 (하단 탭)

- **현황** — 12개 지점사업소를 **1·2·3군**으로 분류해 한눈에 표시(카드는 사업소명 + 보고 건수). 카드를 누르면 **그 사업소의 보고서를 날짜별(최근순)** 로 보여주며, 보고를 누르면 상세·댓글로 이동. 하단에 사업소 전략 정보(지분율·핵심 이슈·대응방안)도 함께 표시.
- **보고** — 관리소장이 **월례 업무보고**를 작성. **0.입주현황(전체호실·입주호실·입주율 자동계산)** + 6개 항목(①관리단 구성 동정 ②우호 인원 섭외 ③래포 형성 ④추진위 미팅 ⑤시행/시공사 ⑥계획·착안). 보고별 **댓글 스레드**로 본사와 소통.
- **사업소 추가/군 이동(본사 담당자)** — 현황 화면에서 본사 토큰 로그인 시 ‘＋ 사업소 추가’ 버튼(사업소명·분류군·등기율)이 보입니다. 또한 사업소 상세를 열면 **‘분류(군) 이동’** 세그먼트로 해당 사업소를 1·2·3군 간에 즉시 이동할 수 있고, 변경은 `branches.json`에 저장되어 모든 사용자에게 공유됩니다.
- **전략** — 기본 개념(관리소장 80% / 본사 20%), 개념도, 군 분류 기준.

### 데이터 파일

- `assets/data/branches.json` — 12개 사업소 현황·전략 데이터. **표의 등기율·성향 등 일부 값은 PDF에서 자동 추출한 것으로, 본사 확인 후 이 파일을 수정**하세요.
- `assets/data/branch-reports.json` — 월례 보고 저장소(보고마다 `comments` 배열 포함).

### 보고 열람 (본사 담당자 전용)

‘보고’ 탭의 **전체 사업소 보고 열람은 본사 토큰 로그인이 필요**합니다(별도 암호 없음). 관리소장은 로그인 없이 **보고 작성**만 가능하고, 사업소별 보고 이력은 현황에서 확인할 수 있습니다.


### 보고 저장 방식 (3단계)

정적 사이트라 "로그인 없는 쓰기"를 위해 순서대로 동작합니다.

1. **중앙 저장 엔드포인트(권장)** — 토큰을 서버에 숨기는 프록시 설정 시, 소장이 **로그인 없이** 올린 보고·댓글이 모두 **중앙 기록**되어 본사·전 사업소가 공유.
   `manage.html` `<head>` 에 `<script>window.ACE_REPORT_ENDPOINT='https://your-worker.workers.dev';</script>` 추가(Worker 예시는 위 CASTING VOTE 안내의 Worker 코드와 동일 구조, 단 PATH를 `assets/data/branch-reports.json` 으로 변경).
2. **본사 관리자 토큰** — 헤더의 **‘본사’** 버튼으로 GitHub 토큰 로그인 시 직접 커밋·삭제.
3. **로컬 기록 + 메일 전송(기본 폴백)** — 위 둘이 없으면 보고가 그 기기에 기록되고 본사에는 메일로 전달(앱은 바로 동작, 보고 화면 상단에 안내 표시).

## 로컬 미리보기

```bash
# 간단한 정적 서버 실행 (Python 3)
python3 -m http.server 8000
# 브라우저에서 http://localhost:8000 접속 (앱: http://localhost:8000/manage.html)
```
